package com.practise;

public interface Vehicle {
    
    String brand="honda";//by default variables are final,static,public
    void mileage(); //by default methods are abstract in interface 
    abstract void wheels(); //by default members are public
}
